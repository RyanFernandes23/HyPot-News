implementation plan (frontend and backend)

morning briefing screen:

to achieve this we need to build a backend api that will fetch the news from newsapi and store it in the database

and a frontend app that will display the news in a slideshow

we also need to build a backend api that will fetch the news from newsapi and store it in the database

and a frontend app that will display the news in a slideshow